name: Deploy Service

on:
  push:
on: [push]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Deploy
      run: ./scripts/deploy.sh
      env
        DB_PASSWORD: ${{ secrets.DB_PASSWORD }} # SYNTAX ERROR: missing colon after env
